spring:
  datasource:
    url: jdbc:mysql://localhost:3306/prueba4?useSSL=false
    username: root 
    password: dante.canizo
  jpa:
    show-sql: false 
    hibernate:
      ddl-auto: update 
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
server:
  port: 8080